# apache.yml
name: apache1
repo: https://github.com/AlissonMMenezes/Terminus.git
commands:
  - apt-get clean
  - apt-get update
  - apt-get -f install apache2 -y
  - service apache2 restart
  - pserver --daemon 
